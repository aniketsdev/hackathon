# Script

Stop risky pull requests before they merge.

ComplyPatch watches GitHub webhooks, scans the changed code, and turns compliance risk into evidence.

Secrets. Patient logs. Missing auth. Unsafe SQL.

The review comment is ready before the merge button is.

Open the console. Connect the webhook. Let every pull request prove it is safe.
